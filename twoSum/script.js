const twoSum = (numbers, target) => {
    let hash = {};

    while (low < high) {
        if (numbers[low] + numbers[high] === target) {
            return true;
        }
    }
};


console.log([3, 2, 4], 6)